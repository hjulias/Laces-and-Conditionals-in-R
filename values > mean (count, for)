 #Crie um script que retorne os números que são maiores que a média do vetor. Use "count" e "for"
 x <-c(1,3,2,4,5,6,8,9)
  count <- 0
  for (i in x)
    if(i > mean(x)){
      print((count<- i))
  } 
